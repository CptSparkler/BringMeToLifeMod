Changes from the original mod to allow multiple items to be configured and used for revival in the config.json using itemids.

NOTE* It will consume the whole item, regardless of use charges unless testing mode is enabled.
